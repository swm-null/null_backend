spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}
  ai:
    url: ${AI_URL}

swagger:
  server-url: ${SWAGGER_SERVER_URL}

cors:
  allowedOrigins:
    - ${ALLOWED_ORIGINS_DEVELOP}
    - ${ALLOWED_ORIGINS_LOCAL:http://localhost:3000}
