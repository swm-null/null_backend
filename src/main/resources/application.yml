spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}
  ai:
    url: ${AI_URL}

jwt:
  secret-key: cd06f8c2b0dd065faf6ef910c7f15934363df71c33740fd245590665286ed268
  access-token:
    expiration-time: 600000
  refresh-token:
    expiration-time: 86400000

swagger:
  server-url: ${SWAGGER_SERVER_URL}

cors:
  allowedOrigins:
    - ${SWAGGER_SERVER_URL}
    - ${ALLOWED_ORIGINS_PRODUCT}
    - ${ALLOWED_ORIGINS_STAGE}
    - ${ALLOWED_ORIGINS_LOCAL:http://localhost:3000}
